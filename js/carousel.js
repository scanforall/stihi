const slider = $('.carousel__slider').bxSlider({
  mode: 'horizontal',
  autoDirection: 'next',
  minSlides: 4,
  maxSlides: 5,
  slideWidth: 3000,
  ticker: true,
  speed: 50000,
  slideMargin: 0,
  adaptiveHeight: true,
});
