const slider = $('.carousel__slider').bxSlider({
  mode: 'horizontal',
  autoDirection: 'next',
  minSlides: 8,
  maxSlides: 8,
  slideWidth: 3000,
  ticker: true,
  speed: 50000,
  slideMargin: 0,
  adaptiveHeight: true,
});
